version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install 
  build:
    commands: 
      - node -v
      - npm -v
      - tsc -v
      - npm run deploy
artifacts:
  files:  
    - '**/*'